{
  "name": "http-reload-proxy",
  "description": "HTTP/1.1 Proxy server that auto-reloads HTML pages when files in a directory change.",
  "license": "Apache-2.0",
  "readme": "README.md",
  "type": "module",
  "scripts": {
    "test": "node --experimental-eventsource --test",
    "start": "node src/index.js"
  }
}